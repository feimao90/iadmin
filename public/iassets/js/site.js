layui.config({base: '/iassets/js/'}).use(['element', 'layer', 'tab'], function() {

    var element = layui.element(),
        $ = layui.jquery,
        layer = layui.layer,
        tab = layui.tab({
            elem: '.admin-nav-card' //设置选项卡容器
        });
})